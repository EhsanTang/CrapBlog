package cn.crap.blog.service.imp;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import cn.crap.blog.dao.imp.%sDao;
import cn.crap.blog.domain.dao.%s;
import cn.crap.blog.service.I%sService;
@Service
public class %sService implements I%sService{

	@Autowired
	private %sDao dao;
	@Override
	public List<%s> selectByIds(List<Long> ids) {
		if(ids == null || ids.size() == 0){
			return new ArrayList<%s>();
		}
		return dao.selectByIds(ids);
	}
}
